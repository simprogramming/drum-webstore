<%= render 'shared/navbar' %>
<div class="banner">
  <h2>Drum FX</h2>
</div>

<div class="container my-5 shop">
  <p class="text-left">Drum Shop</p>
</div>

</hr>

<div class="container">
  <div class="cards">
    <% @items.each do |item| %>
      <%=  link_to (image_tag item.pictures) , item_path(item) %>
    <% end %>
  </div>
</div>

